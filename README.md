# React Profiler Examples:
A Tutorial for developers to get acquainted with the React Profiler and go through some basic examples of spotting unoptimized code.

# Run Locally:
npm install && npm run build && npm start
